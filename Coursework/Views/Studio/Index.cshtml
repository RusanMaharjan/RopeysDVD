@model System.Collections.Generic.IEnumerable<Coursework.Models.Studio>

@{
    ViewBag.Title = "All Studio";
    Layout = "_Layout";
}

<div class="row mt-4">
    <div class="col-md-6"><h2>All Studio</h2></div>
    <div class="col-md-6 text-end"><a type="button" asp-controller="Studio" asp-action="Create" class="btn btn-outline-light btn btn-lig">Create Studio <i class="bi bi-plus-circle"></i></a></div>
</div>


<table class="table text-center table-bordered table-striped w-100 mb-5">
    <thead class="bg-secondary">
    <tr>
        <th>Studio Name</th>
        <th>Action</th>
    </tr>
    </thead>
    
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.StudioName</td>
            <td role="group" class="text-center">
                <a class="btn btn-info text-decoration-underline mx-3" asp-controller="Studio" asp-action="Edit" asp-route-id="@item.StudioNumber">Edit &rarr;</a>
                <a class="btn btn-danger text-decoration-underline" asp-controller="Studio" asp-action="Delete" asp-route-id="@item.StudioNumber">Delete &rarr;</a>
            </td>
        </tr>
    }
    </tbody>
</table>
