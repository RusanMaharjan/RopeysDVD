@model System.Collections.Generic.IEnumerable<Coursework.Models.Studio>

@{
    ViewBag.Title = "All Studio";
    Layout = "_Layout";
}

<h2>All Studio</h2>

<table class="table table-bordered table-striped w-100">
    <thead>
    <tr>
        <th>Studio Name</th>
        <th>Action</th>
    </tr>
    </thead>
    
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.StudioName</td>
            <td role="group" class="text-center">
                <a class="btn btn-warning mx-3" asp-controller="Studio" asp-action="Edit" asp-route-id="@item.StudioNumber"><i class="bi bi-pencil-square fs-3"></i></a>
                <a class="btn btn-danger" asp-controller="Studio" asp-action="Delete" asp-route-id="@item.StudioNumber"><i class="bi bi-trash fs-3"></i></a>
            </td>
        </tr>
    }
    </tbody>
</table>
